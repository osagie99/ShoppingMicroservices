server:
  port: 9000

spring:
  application:
    name: AUTHENTICATION-SERVICE
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/userDB
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  config:
   import: configserver:http://localhost:9296

logging:
  level:
    org:
      springframework:
        web:
          servlet:
            DispatcherServlet: DEBUG
  pattern:
    level: "%5p [${spring.application.name:},%X{traceId:-},%X{spanId:-}]"
management:
  tracing:
    sampling:
      probability: 1.0

SECRET_KEY: 3677397A24432646294A404E635266556A586E327235753778214125442A472D